/* global App */

/* exported testAppCreateNextBook */
/**
 * Тестовая функция для `app.createNextBook()`
 */
function testAppCreateNextBook() {
  const app = new App();
  app.createNextBook();
}

/* exported testAppResetName */
/**
 * Тестовая функция для `app.resetName()`
 */
function testAppResetName() {
  const app = new App();
  app.resetName();
}
